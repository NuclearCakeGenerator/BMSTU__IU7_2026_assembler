{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-asm",
            "type": "shell",
            "command": "make",
            "group": "build",
            "options": {
                "cwd": "${fileDirname}"
            }
        }
    ]
}
